name: Roya refresh scheduler B

on:
  schedule:
    - cron: "22 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    uses: ./.github/workflows/refresh.yml
    permissions:
      contents: write
