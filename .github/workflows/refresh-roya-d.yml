name: Roya refresh scheduler D

on:
  schedule:
    - cron: "52 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    uses: ./.github/workflows/refresh.yml
    permissions:
      contents: write
